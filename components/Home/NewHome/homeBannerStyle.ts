/**
 * Corner radius shared by every card in the home banner column — the CTA
 * banners (Figma 25141:6965), the cashback banner and the promo slot below them.
 *
 * One constant because the three sit stacked in the same column: a banner drawn
 * at a different radius from the one above it is immediately obvious.
 */
export const HOME_BANNER_RADIUS = 23;
