import { Text } from '@/components/ui/text';

const SavingDepositDescription = () => {
  return (
    <Text className="text-center md:text-start text-lg md:text-2xl font-medium opacity-70 max-w-72 md:max-w-[23rem] mx-auto md:mx-0">
      Start earning real yield today. Deposit once, withdraw anytime
    </Text>
  );
};

export default SavingDepositDescription;
