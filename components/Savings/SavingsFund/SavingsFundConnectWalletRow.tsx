// Shadowed by SavingsFundConnectWalletRow.web / .native on every platform that
// ships — this base file exists for tsc, which does not platform-resolve.
export { default } from './SavingsFundConnectWalletRow.web';
